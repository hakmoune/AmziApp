import { Button } from "@/components/ui/button";
import { Printer } from "lucide-react";

const PrintClasseur = () => {
  return (
    <Button variant="outline">
      <Printer /> Imprimer
    </Button>
  );
};

export default PrintClasseur;
